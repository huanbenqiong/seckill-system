# 高并发秒杀系统 · 微服务 + Agent 智能增强 — 答辩演讲稿

> 说明：共 20 页，主讲 **高俊**；第 13–15 页（LLM Router）由 **赵紫东** 讲。
> 每页给出可直接朗读的口播稿 + 圆括号里的小提示。建议总时长 10–12 分钟。
> 翻页：方向键 / 空格；视频页点击卡片放大播放，Esc 关闭。

---

## 第 1 页 · 封面
各位老师好，我们小组的课程设计题目是《高并发秒杀系统——微服务 + AI Agent 智能增强》。
这个项目分两层：底层是一套基于 Spring Cloud Alibaba 的高并发秒杀交易系统，保证"卖得稳、不超卖"；上层我们叠加了一个由大模型驱动的智能运营 Agent，能把一句自然语言指令，直接变成对后端微服务的真实操作。
我是主讲高俊，Agent 的大模型路由部分由我的队友赵紫东讲解。

## 第 2 页 · 提纲与分工
我们的汇报分四条主线：第一是项目与微服务架构；第二是高并发内核的三大关键技术和压测；第三是 Agent 智能层，这也是我们的核心亮点；第四是演示与总结。
其中前两部分和业务 Agent 由我讲，赵紫东负责讲 LLM Router 的原理。整场最想让老师看到的，是两点：微服务怎么扛住高并发，以及 Agent 怎么让系统"会思考、能动手"。

## 第 3 页 · 背景与目标
我们面对两个挑战。第一个是高并发交易：秒杀开始的瞬间可能有上万请求涌进来，库存只有 100 件就绝不能卖出 101 件，而且扣库存和建订单跨了 Redis 和数据库，还要保证最终一致。
第二个是运营决策：传统系统只会产出订单数据，却不会给建议——查库存、补货、看大盘全靠人工敲 SQL。
我们的思路是：用微服务解决"高并发安全交易"，用 AI Agent 解决"智能运营决策"，两层物理隔离、各自演进。

## 第 4 页 · 整体架构
这是系统的整体架构。最上面是 React 前端；所有请求先经过网关 seckill-gateway，它统一做 JWT 鉴权和基于 Redisson 的分布式限流。
往下拆成五个微服务：用户、商品、订单，再加一个智能数据代理 admin-agent。服务之间通过 Nacos 注册发现、用 OpenFeign 互相调用；秒杀链路则通过 RocketMQ 异步解耦。
最底层是 MySQL、Redis、RocketMQ、Nacos 四大中间件。要强调的是：Agent 层只读数据，和交易核心是零侵入的。

## 第 5 页 · 技术栈
技术选型上我们用的都是工业界主流方案：后端是 Java 17 + Spring Boot 3.2.5 + Spring Cloud Alibaba；高并发靠 Redis 的 Lua 原子脚本、Redisson 的分布式锁和令牌桶、以及 RocketMQ 异步削峰；网关是 Spring Cloud Gateway，配 JWT 和雪花 ID。
前端是 React + TypeScript。AI 这块有两部分：一个是 Python 的业务 Agent，接 DeepSeek 大模型做工具调用；另一个是赵紫东用 C# 写的 LLM Router，做本地模型的统一接入。

## 第 6 页 · 秒杀链路五关卡
一次秒杀请求要过五道关卡。第一关是网关的限流和鉴权，秒杀路径限到每个 IP 50 QPS，超了直接返回 429。第二关是 Redis Lua 原子扣减，"检查库存加扣减"一步完成，亚毫秒级，杜绝超卖。第三关，扣减成功后发一条 RocketMQ 消息就立即返回，不等数据库。第四关，订单服务消费消息时用分布式锁加订单唯一做双重幂等。第五关，订单入库后回调同步数据库库存，保证最终一致。
一句话概括我们的设计哲学：能挡的挡在外面，能快的全放内存，能异步的都异步，能防的多重防。

## 第 7 页 · 防超卖（Redis Lua）
这页讲防超卖。难点其实是"读"和"写"之间的时间窗口——多个线程同时读到"还剩 1 件"，就会都去扣减，造成超卖。数据库的锁是为一致性设计的，不是为高吞吐设计的。
我们的做法是把"检查库存"和"扣减库存"放进同一段 Lua 脚本，交给 Redis 执行。Redis 是单线程的，这段脚本天然原子、没有竞态。库存提前预热到内存，数据库完全不参与热路径。最终结果就是超卖率为零，而且吞吐反而更高。

## 第 8 页 · 异步解耦 + 双重幂等
左边是异步解耦：库存扣减成功后只发一条 MQ 消息就立即返回，订单异步创建，起到削峰填谷的作用，用户感知到的延迟基本只等于一次 Redis 操作。
右边是幂等：因为 RocketMQ 是至少投递一次，可能重复消费，所以我们设了两道防线——先用 Redisson 分布式锁锁住"同一商品同一用户"，再用订单主键唯一兜底。这样即使消息重复投递，也绝不会重复下单。这是我们排查到重复订单后总结出来的经验：分布式幂等从来不是单一银弹，而是多道防线。

## 第 9 页 · 并发压测结果
这是我们的压力测试结果。左边表格是秒杀核心在不同并发档位下的吞吐：并发 500 时达到峰值 1840 QPS，成功率 100%，零错误，P99 时延只有 360 毫秒；并发拉到 800 依然稳定。
右边是稳定性与防护：全程没有任何 5xx 或超时、系统零雪崩；Redis Lua 保证零超卖；网关令牌桶把突发的超额请求用 429 平滑拒绝；P99 时延随并发平稳增长，没有毛刺。
三个核心数字：峰值 1840 QPS、零超卖零错误零雪崩、100% 成功率。

## 第 10 页 · Agent 核心亮点（高俊）
接下来是我们最想展示的核心亮点——当 Agent 遇上微服务。
传统运营要点按钮、敲 SQL；而在我们的系统里，运营人员只需要对着 AI 助手说一句话，比如"帮我把库存低于 10 的商品全部补到 50"。
背后是：大模型理解意图，自动选择工具，调用真实的微服务接口，去操作 Redis 和数据库，最后再把结果用人话回报。它能做库存洞察、一键补货、看经营大盘、销售排行、活动延期，还支持多轮对话。这就是"微服务 + Agent"真正的价值。

## 第 11 页 · Agent 工作原理（高俊）
这页拆解 Agent 是怎么把"一句话"变成"真实操作"的闭环，一共五步：
第一步，前端 AI 助手把自然语言发给 Python Agent；第二步，DeepSeek 大模型通过 Function Calling 判断意图、选好要调用的工具和参数；第三步，工具用 HTTP 带着令牌去调 admin-agent 的 REST 接口；第四步，admin-agent 真正操作 Redis 和 MySQL，返回结构化结果；第五步，大模型把结果整理成自然语言回复。
核心一句话：Agent = 大模型负责决策 + 工具负责行动，微服务的接口就是 Agent 的"手脚"。

## 第 12 页 · Agent 架构（高俊）
这是 Agent 的架构图。最上面是前端的 AI 商家助手浮窗，可以拖动；它把对话发到运行在 8099 端口的 Python Agent，里面是 DeepSeek 大模型加上 stock_tools、data_tools 两组工具；这些工具再通过带令牌的 REST 调用我们的 admin-agent 微服务；admin-agent 最终读写 Redis 和 MySQL。
这里体现的是关注点分离：大模型负责"听懂和决策"，admin-agent 负责"安全地执行"，而交易核心完全不受影响。

## 第 13 页 · LLM Router 定位（赵紫东）
（赵紫东讲）这部分是我负责的 C# Agent LLM Router。它是一个独立的 ASP.NET Core 服务，定位是"统一的大模型网关"，对接 Jan.ai 的本地模型，供 Java 或 Python 侧调用。
它解决三个问题：一是屏蔽底层模型差异，业务侧永远只调一个 /api/agent/chat 接口；二是支持运行时热切换模型，不用改代码也不用重启；三是用 OpenAI 兼容协议，将来能平滑接入云端模型。再加上内置容错，让大模型调用从"能用"升级到"可靠"。

## 第 14 页 · LLM Router 架构与切换（赵紫东）
（赵紫东讲）Router 内部分四个模块：LlmRouter 是调度中心，负责选模型和路由；JanLlmService 用 OpenAI 兼容格式去调 Jan.ai；ResilienceHandler 基于 Polly 做重试和熔断；ConfigLoader 负责热加载配置里的模型列表。
模型怎么选有四级优先级：先看请求体里的 model 字段，没有就看请求头 X-Model，再看 Query 参数，最后才用配置文件里的默认模型。这样调用方可以在不同粒度上灵活控制用哪个模型。

## 第 15 页 · LLM Router 韧性机制（赵紫东）
（赵紫东讲）这页讲韧性。重试用的是指数退避：遇到 5xx、429 或连接失败这类临时故障，自动重试，间隔 1 秒、2 秒、4 秒，最多 3 次。瞬时抖动一般一次重试就恢复了。
熔断是个状态机：连续失败 5 次就打开熔断、直接快速失败，不再打到后端；休眠 30 秒后进入半开状态试探，成功就恢复，失败就继续熔断。这样当模型后端过载时，Router 快速失败而不是堆积请求，既保护调用方也保护模型本身。此外还做了 API Key 认证、请求 ID 全链路追踪和标准错误格式。

## 第 16 页 · 商家数据中心可视化（高俊）
回到我这边的业务展示。这是商家端的数据中心看板：上面是实时的经营概览，商品数、已售、订单、待支付和销售额，每 3 秒自动刷新；下面是数据可视化——近 7 日销售趋势的面积图、商品分类销量占比的饼图，还有近 7 日订单量的柱状图，都是用 Recharts 渲染的。
（指向截图）这样商家一进后台，经营状况一目了然。

## 第 17 页 · 演示视频（高俊）
下面我们用三段实机录屏来演示。
（点击第一段）第一段是用户购买的完整流程：登录、浏览商品、秒杀、下单、支付。
（播放后关闭，点第二段）第二段是商家发布商品：进商家后台，填写信息、上传图片、上架。
（点第三段）第三段是本项目的亮点——Agent 自动补货和数据分析：我对 AI 助手说一句话，它就自动完成了低库存查询、批量补货，并给出数据分析。
（视频可点击放大播放，Esc 关闭）

## 第 18 页 · 微服务关键设计要点（高俊）
这页总结微服务架构下我们做的四个关键设计：第一，服务注册与通信，用 Nacos 做注册发现、OpenFeign 做声明式调用，让各服务解耦自治；第二，高并发防超卖，商品服务用 Redis Lua 原子脚本做到零超卖；第三，异步解耦，用 RocketMQ 在商品和订单服务之间削峰、保证最终一致；第四，Agent 编排服务，用大模型的 Function Calling 去调度这些微服务接口。
一句话：微服务把复杂系统拆成可独立演进的服务，Agent 再用标准接口把它们智能地编排起来——这就是我们项目的核心设计思想。

## 第 19 页 · 团队分工
这是我们的分工：我（高俊）负责整体架构、网关鉴权与限流、业务 Agent 以及整体集成和压测；赵紫东负责 C# 的 LLM Router，也就是模型路由、切换和熔断；李恩云负责商品和订单服务、RocketMQ 消费和 Redisson 分布式锁；陈宇轩负责 React 前端、admin-agent 数据代理和数据可视化。
四个人协作，覆盖了从网关、交易内核、异步消息、到 Agent 和前端的完整链路。

## 第 20 页 · 总结与展望
最后总结一句话：微服务负责"稳"，Agent 负责"智"。
我们用工业级的微服务方案守住了高并发交易的稳——实测峰值 1800 多 QPS、零错误零超卖；又用大模型 Agent 把运营从"人工敲 SQL"升级为"一句话搞定"的智能决策。两层通过标准接口协作、互不侵入，也为以后叠加更多智能能力留足了空间。
未来我们计划加入 WebSocket 实时预警、Agent 自动巡检和分布式压测调优。
我们的汇报到此结束，谢谢各位老师，敬请指正！

---

### 提问环节可能用到的备答
- **为什么不直接用数据库锁防超卖？** 数据库锁为事务一致性设计，高并发下行锁竞争严重、吞吐骤降；Redis 单线程 + Lua 原子脚本在内存完成，亚毫秒级且零竞态。
- **Agent 会不会误操作核心数据？** 不会。Agent 只通过 admin-agent 暴露的、带 Token 鉴权的只读/受控接口操作，且与交易核心物理隔离，不触碰下单链路。
- **两个大模型（DeepSeek / Jan.ai）什么关系？** DeepSeek 是业务 Agent 当前用的云端模型；Jan.ai + C# Router 是可切换的本地模型接入层，体现"模型可插拔"的设计。
- **限流为什么是每 IP 100 QPS？** 这是演示配置值，可在 Nacos 配置中心动态调整；真实生产会结合全局限流和热点参数限流。
